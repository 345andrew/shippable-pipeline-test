language: node_js

node_js:
  - "6.9.1"

services:
  - mongodb

branches:
  only:
    - master

build:
  cache: true
  cache_dir_list:
    - $SHIPPABLE_BUILD_DIR/node_modules
  pre_ci:
    - sudo pip install awscli
  ci:
    - npm install --unsafe-perm
  post_ci:
